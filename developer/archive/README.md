# Developer-only archive

Files here are for maintainers and local verification only. They are not part of any Eve app project, are not discovered as Eve agent resources, and must not be exposed as app-user tools or routes.

The archived Pi compatibility adapter is retained for developer experiments. New app-user flows use Eve's native `experimental_workflow` and declared subagents instead.
